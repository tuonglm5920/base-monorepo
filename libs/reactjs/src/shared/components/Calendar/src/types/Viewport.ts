export interface Viewport {
  /** The month represented as a zero-based index (0 for January, 1 for February, etc.) */
  month: number;

  /** The year represented as a four-digit number (e.g., 2023) */
  year: number;
}
