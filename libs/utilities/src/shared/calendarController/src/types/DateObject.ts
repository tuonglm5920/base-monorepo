export interface DateObject {
  /** The day of the month (1-31). */
  day: number;

  /** The month of the year (0-11), where 0 represents January. */
  month: number;

  /** The year, represented in full (e.g., 2023). */
  year: number;
}
