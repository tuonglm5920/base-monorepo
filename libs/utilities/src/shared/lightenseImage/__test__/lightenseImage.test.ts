describe('LightenseImage', () => {
  it('TODO: Should render correctly when click to open', () => {
    expect(true).toEqual(true);
  });
  it('TODO: Should render correctly when click to close', () => {
    expect(true).toEqual(true);
  });
  it('TODO: Should close correctly when scroll', () => {
    expect(true).toEqual(true);
  });
  it('TODO: Should render correctly when resize', () => {
    expect(true).toEqual(true);
  });
});
